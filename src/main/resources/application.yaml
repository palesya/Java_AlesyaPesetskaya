server:
  port: 8091

spring:
  mvc:
    view:
      suffix: .jsp
      prefix: /WEB-INF/jsp/

  datasource:
    username: postgres
    password: alesya
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost/postgres

  jpa:
    hibernate:
      ddl-auto: 'create-drop'

  h2:
    console:
      enabled: true

